<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:EnvironmentalApp"
             xmlns:data="clr-namespace:EnvironmentalApp.Data"
             x:Class="EnvironmentalApp.SensorManagementPage"
             Title="Sensor Management">

    <Grid>
        <CollectionView x:Name="SensorsCollectionView"
                        ItemsSource="{Binding Sensors}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="data:Sensor">
                    <Frame Padding="10" Margin="5" HasShadow="True" CornerRadius="10">
                        <Frame.Triggers>
                            <DataTrigger TargetType="Frame" Binding="{Binding IsAnomalous}" Value="True">
                                <Setter Property="BackgroundColor" Value="MistyRose" />
                            </DataTrigger>
                        </Frame.Triggers>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <!-- For anomaly reason -->
                            </Grid.RowDefinitions>

                            <Label Text="{Binding Name}"
                   FontAttributes="Bold"
                   Grid.Row="0"
                   Grid.ColumnSpan="2" />

                            <Label Text="Type:"
                   Grid.Row="1"
                   Grid.Column="0" />
                            <Label Text="{Binding Type}"
                   Grid.Row="1"
                   Grid.Column="1" />

                            <Label Text="Status:"
                   Grid.Row="2"
                   Grid.Column="0" />
                            <Label Text="{Binding Status}"
                   Grid.Row="2"
                   Grid.Column="1" />

                            <Label Text="{Binding AnomalyReason}"
                   TextColor="Red"
                   FontAttributes="Italic"
                   IsVisible="{Binding IsAnomalous}"
                   Grid.Row="3"
                   Grid.ColumnSpan="2" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
