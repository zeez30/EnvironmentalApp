<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:EnvironmentalApp"
             xmlns:data="clr-namespace:EnvironmentalApp.Data"
             x:Class="EnvironmentalApp.SensorManagementPage"
             Title="Sensor Management">

    <Grid>
        <CollectionView x:Name="SensorsCollectionView"
                        ItemsSource="{Binding Sensors}"
                        SelectionMode="Single"
                        SelectionChanged="SensorsCollectionView_SelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="data:Sensor">
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Label Text="{Binding Name}"
                               FontAttributes="Bold"
                               Grid.Row="0"
                               Grid.ColumnSpan="3"
                               VerticalOptions="Center" />

                        <Label Text="Type:"
                               Grid.Row="1"
                               Grid.Column="0"
                               VerticalOptions="Center" />
                        <Label Text="{Binding Type}"
                               Grid.Row="1"
                               Grid.Column="1"
                               VerticalOptions="Center" />

                        <Label Text="Status:"
                               Grid.Row="2"
                               Grid.Column="0"
                               VerticalOptions="Center" />
                        <Label Text="{Binding Status}"
                               Grid.Row="2"
                               Grid.Column="1"
                               VerticalOptions="Center">
                            <Label.Style>
                                <Style TargetType="Label">
                                    <Style.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Online">
                                            <Setter Property="TextColor" Value="Green" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Offline">
                                            <Setter Property="TextColor" Value="Gray" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Malfunctioning">
                                            <Setter Property="TextColor" Value="Red" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Label.Style>
                        </Label>

                        <Button Text="Malfunction"
                                Grid.Row="1"
                                Grid.RowSpan="2"
                                Grid.Column="2"
                                Margin="5"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:SensorManagementPage}}, Path=MarkMalfunctioningCommand}"
                                CommandParameter="{Binding .}"
                                IsVisible="False" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <VerticalStackLayout Padding="10" VerticalOptions="End">
            <Label Text="Add New Sensor" FontAttributes="Bold" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Entry Placeholder="Name" x:Name="NewSensorNameEntry" Grid.Column="0" />
                <Entry Placeholder="Type (Air/Water/Weather)" x:Name="NewSensorTypeEntry" Grid.Column="1" />
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Entry Placeholder="Latitude" Keyboard="Numeric" x:Name="NewSensorLatitudeEntry" Grid.Column="0" />
                <Entry Placeholder="Longitude" Keyboard="Numeric" x:Name="NewSensorLongitudeEntry" Grid.Column="1" />
            </Grid>
            <Button Text="Add Sensor" Clicked="AddSensor_Clicked" />
        </VerticalStackLayout>
    </Grid>

</ContentPage>